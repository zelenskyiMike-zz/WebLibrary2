@using WebLibrary2.Domain.Models
@model GetLiteratureViewModel

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@{Html.EnableClientValidation();}
@{Html.EnableUnobtrusiveJavaScript();}
@using (Html.BeginForm())
{


    <div class="form-horizontal">

        <div class="form-row">
            <div class="col">
                <p>Выберите статьи,что бы удалить их для этого автора</p>
                @foreach (var item in Model.Articles)
                {
                    <input type="checkbox" name="articlesIDsForDelete" value="@item">
                    @Html.DisplayFor(modelItem => item.BookName)<br />
                }
            </div>
            <div class="col">
                <p>Выберите статьи,что бы добавить их для этого автора</p>
                @Html.ListBox("articlesIDsForInsert", ViewData["Books"] as MultiSelectList, htmlAttributes: new { @class = "form-control" })
            </div>
        </div>


        <div class="form-row">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Добавить книги" class="btn btn-light" />
            </div>
        </div>
    </div>
}

