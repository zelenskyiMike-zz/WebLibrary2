@using WebLibrary2.Domain.Models
@model GetM2MCRUDAuthorVM

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@{Html.EnableClientValidation();}
@{Html.EnableUnobtrusiveJavaScript();}
@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    <div class="form-horizontal">

        <div class="form-row">
            <div class="col">
                <p>Выберите журналы,что бы удалить их для этого автора</p>
                @foreach (var item in Model.Magazines)
                {
                    <input type="checkbox" name="magazinesIDsForDelete" value="@item">
                    @Html.DisplayFor(modelItem => item.MagazineName)<br />
                }
            </div>
            <div class="col">
                <p>Выберите журналы,что бы добавить их для этого автора</p>
                @Html.ListBox("magazinesIDsForInsert", ViewData["Magazines"] as MultiSelectList, htmlAttributes: new { @class = "form-control" })
            </div>
        </div>


        <div class="form-row">
            <div class="col-md-offset-2 col-md-10">
                <br />
                <input type="submit" value="Подтвердить" class="btn btn-light" />
                @Html.ActionLink("Назад к списку", "AuthorView", "Author", null, new { @class = "btn btn-light" })
            </div>
        </div>
    </div>
}
